# Tasks: Python RAG Backend

## Phase 1: Setup (Shared Infrastructure)

- [X] T001 Create `backend/` directory at the project root.
- [X] T002 Create `backend/.gitignore` to exclude `.env`, `__pycache__`, and `.venv`.
- [X] T003 Initialize a Python virtual environment in `backend/.venv`.
- [X] T004 Install dependencies `fastapi`, `uvicorn`, `qdrant-client`, `cohere`, `openai`, `python-dotenv` into the virtual environment.
- [X] T005 Create `backend/.env` with placeholders for `COHERE_API_KEY`, `QDRANT_URL`, `QDRANT_API_KEY`, and `OPENAI_API_KEY`.

---

## Phase 2: User Story 1 - Contextual Chat Responses (Priority: P1) 🎯 MVP

**Goal**: As a user, I want to ask questions and receive answers based on the textbook's content.

**Independent Test**: Send a POST request to the `/chat` endpoint with a question. The response should be a text answer generated by the RAG pipeline, consistent with the content in the `docs` folder.

### Implementation for User Story 1

- [X] T006 [US1] Create `backend/ingest.py` to load documents from `../docs/**/*.mdx`.
- [X] T007 [US1] Implement embedding and storage logic in `backend/ingest.py` to connect to Qdrant, re-create the `robotics_textbook` collection, and upload embedded documents using Cohere.
- [X] T008 [US1] Create `backend/server.py` and initialize a FastAPI application.
- [X] T009 [US1] Define a `ChatRequest` Pydantic model in `backend/server.py`.
- [X] T010 [US1] Implement the `@app.post("/chat")` endpoint in `backend/server.py`.
- [X] T011 [US1] Add RAG logic to the `/chat` endpoint in `backend/server.py` to embed the query, search Qdrant for context, and generate a response with OpenAI.

---

## Phase 3: Polish & Cross-Cutting Concerns

- [X] T012 Create `backend/README.md` with instructions on setup, environment configuration, and how to run `ingest.py` and `server.py`.

---

## Dependencies & Execution Order

- **Phase 1 (Setup)** must be completed before starting Phase 2.
- **Phase 2 (User Story 1)** depends on the completion of Phase 1.
- **Phase 3 (Polish)** can be done after Phase 2 is complete.

---

## Implementation Strategy

1.  Complete all tasks in **Phase 1** to set up the environment.
2.  Implement the ingestion and API logic in **Phase 2**.
3.  Document the process in **Phase 3**.

This represents the MVP for the backend service.
